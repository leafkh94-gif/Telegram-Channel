"""
config.py — الاتصال والأدوات فقط

لا معاملات استراتيجية هنا بعد الآن. حُذفت كلها مع الاستراتيجية القديمة،
وتُضاف الجديدة حين تُقاس لا حين تُقترح.
"""

import os
from dotenv import load_dotenv

load_dotenv()

# ─── Capital.com API ───────────────────────────────────────────────────────────
# .strip() ضروري وليس تجميلاً: أسرار GitHub غالباً فيها سطر جديد زائد، و requests
# يرفض قيمة هيدر تحتوي "\n" برمي ValueError عند الإقلاع. هذا بالضبط ما سبّب
# انهياراً متكرراً سابقاً — كل تشغيل يموت خلال ثوانٍ ويُعيد إطلاق نفسه، فوصلتك
# رسالة "البوت يعمل" كل ثانية تقريباً.
CAPITAL_API_KEY    = os.getenv("CAPITAL_API_KEY", "").strip()
CAPITAL_PASSWORD   = os.getenv("CAPITAL_PASSWORD", "").strip()
CAPITAL_IDENTIFIER = os.getenv("CAPITAL_IDENTIFIER", "").strip()
CAPITAL_DEMO       = os.getenv("CAPITAL_DEMO", "true").strip().lower() == "true"
# الرابط يتحدد تلقائياً حسب demo/live
if CAPITAL_DEMO:
    CAPITAL_BASE_URL = "https://demo-api-capital.backend-capital.com/api/v1"
else:
    CAPITAL_BASE_URL = "https://api-capital.backend-capital.com/api/v1"

# ─── Telegram ─────────────────────────────────────────────────────────────────
# اسم السرّ في هذا المستودع هو TELEGRAM_BOT_TOKEN (وهو ما يمرّره الـ workflow).
# قراءة TELEGRAM_TOKEN وحده كانت ستُرجع نصاً فارغاً فيفشل الإرسال بصمت.
TELEGRAM_TOKEN   = (os.getenv("TELEGRAM_BOT_TOKEN", "") or os.getenv("TELEGRAM_TOKEN", "")).strip()
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID", "").strip()

# ─── الأدوات ──────────────────────────────────────────────────────────────────
# الـ epics مطابقة لما يعمل فعلاً على هذا الحساب
# (NASDAQ/DOW/SPX500 كانت تُرجع 404 — نستخدم US100/US30/US500)
# US500 محذوفة بقرار مقاس لا بانطباع: صافي ‎-0.052R للصفقة بعد السبريد،
# سالبة داخل العيّنة (‎-0.033) وخارجها (‎-0.082) وفي كل تركيبة جُرّبت.
SYMBOLS = {
    "US100":  {"epic": "US100",  "pip_size": 1.0,  "min_atr": 5.0},
    "US30":   {"epic": "US30",   "pip_size": 1.0,  "min_atr": 8.0},
    "XAUUSD": {"epic": "GOLD",   "pip_size": 0.1,  "min_atr": 1.0},  # الذهب
}

# ─── الأطر الزمنية ────────────────────────────────────────────────────────────
TIMEFRAME_DAILY = "DAY"
TIMEFRAME_H1    = "HOUR"

CANDLES_DAILY = 120     # يكفي EMA20 بهامش واسع
CANDLES_H1    = 200     # قناة 24 + ATR 14 + هامش لإعادة بناء الصفقات

# ─── معاملات الاستراتيجية ────────────────────────────────────────────────────
# كل رقم هنا خرج من مسح 36 تركيبة على سنتين من شموع الساعة، بخصم السبريد،
# باختيار على أول 16 شهراً واختبار على 8 شهور لم يرها التصميم.
#
# النتيجة على الأدوات الثلاث مجتمعة: 721 صفقة، 2.31 صفقة لكل أداة أسبوعياً،
# إصابة 62.7%، صافي ‎+0.099R للصفقة (t=2.40). داخل العيّنة ‎+0.091R وخارجها
# ‎+0.113R — أي أنها تحسّنت خارج عيّنتها بدل أن تنهار. وحذف أكبر صفقة رابحة
# يُبقيها عند ‎+0.091R، فهي ليست معلّقة على صفقة واحدة.
#
# تحفّظ يجب أن يبقى ظاهراً: الأفضلية المقاسة ليست موزّعة بالتساوي. الذهب
# وحده ‎+0.230R بدلالة إحصائية حقيقية (t=3.08)، بينما US100 (t=0.60) و US30
# (t=0.52) موجبتان لكن غير مميّزتين عن الصفر. تشغيلهما قرار اتُّخذ بعد عرض
# هذه الأرقام، لا استنتاج منها.

DAILY_EMA        = 20    # اتجاه السوق: فوق EMA20 نشتري، تحته نبيع
ENTRY_CHANNEL_H1 = 24    # اختراق أعلى/أدنى 24 شمعة ساعة
ATR_PERIOD       = 14

STOP_ATR           = 1.5   # الوقف المبدئي، وهو وحدة قياس R
TRAIL_ATR          = 3.0   # الوقف المتحرّك
PARTIAL_TARGET_ATR = 1.0   # نخرج بنصف الكمية هنا وننقل الوقف إلى الدخول
PARTIAL_FRACTION   = 0.5

# لا مرشّح 4 ساعات ولا حارس ملاحقة. كلاهما موجود في النسخ السابقة، وكلاهما
# غائب عن القياس الذي أنتج الأرقام أعلاه. إضافة شرط لم يُقَس تجعل البوت
# يتصرّف بغير ما قِيس — وهذا بالضبط ما أعطى سابقاً رقمين متناقضين.

# البيع مسموح. مساهمته أضعف من الشراء (‎+0.054R مقابل ‎+0.122R، وt=0.83 مقابل
# 2.32) لكنه موجب، وهو ما يرفع التكرار من 1.5 إلى 2.3 صفقة أسبوعياً.
ALLOW_SHORT = True

# ─── إدارة المخاطرة ───────────────────────────────────────────────────────────
RISK_PER_TRADE_PCT = 0.01
MAX_OPEN_TRADES    = 3     # أداة واحدة لكل صفقة، وثلاث أدوات

# ─── التشغيل ──────────────────────────────────────────────────────────────────
BOT_MODE              = os.getenv("BOT_MODE", "alert_only").strip()
SCAN_INTERVAL_SECONDS = 60
